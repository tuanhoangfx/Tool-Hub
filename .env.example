# Tool-local env (per-project identity, VITE_* for browser). Gitignored except this example.

# Supabase Management API — shared across P00xx; put the real token in E:\Dev\.env.shared
# (copy from E:\Dev\.env.shared.example). See docs/SUPABASE-QUOTA.md.
# SUPABASE_MANAGEMENT_TOKEN=sbp_…

# Workspace identity (x1z10 P01) — login + user directory on Tool Hub
VITE_SUPABASE_URL=https://fmnrafpzctuhxjaaomzt.supabase.co
VITE_SUPABASE_ANON_KEY=

# Hub UI — S1 Classic (deprecated)

> **Removed 2026-05:** `design-template/hub-ui/` deleted. Use **Hub Card** preview (`dtpl=hub-card`) or production `HubToolCard.tsx`.

## Replacement

| Was | Now |
|-----|-----|
| `dtpl=hub-ui` spectrum mock | `dtpl=hub-card` (Design Template) |
| `ToolCard` + `hub-spectrum.css` | `HubToolCard` + `badge-registry` |

## Historical bands (reference)

1. Identity + status
2. Stack — TagRow
3. Metrics — version, release, branch
4. Health bar
5. Footer — local path, port, quick links
